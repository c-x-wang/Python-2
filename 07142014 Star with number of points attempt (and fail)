# star with specified number of points (attempt)

import turtle
t = turtle.Turtle()

points = int(input("Enter a number of points."))
length = int(input("Enter a side length."))

def star(points, length):
	for i in range(points):
		t.forward(length)
		t.right(720.0 / points)

star(points, length)